# Pinned dependency constraints for reproducible installs.
#
# Install with:  pip install -e '.[dev]' -c constraints.txt
#
# These are version pins (not hashes) captured from a known-good resolved
# environment. They make CI and contributor installs reproducible without
# changing the version *ranges* declared in pyproject.toml. Regenerate after a
# deliberate dependency bump with:  pip freeze (curated to the closure below).
#
# Note: feedparser / sgmllib3k are intentionally NOT pinned here. feedparser is
# an optional [parse] extra; the default install uses the stdlib parser.

# --- runtime (direct + transitive) ---
annotated-types==0.7.0
anyio==4.14.0
certifi==2026.2.25
defusedxml==0.7.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.11
markdown-it-py==4.2.0
mdurl==0.1.2
Pygments==2.20.0
pydantic==2.13.4
pydantic_core==2.46.4
python-dateutil==2.9.0.post0
PyYAML==6.0.1
rich==15.0.0
shellingham==1.5.4
six==1.16.0
typer==0.26.7
typing_extensions==4.15.0

# --- dev / test toolchain ---
coverage==7.14.3
hypothesis==6.155.7
sortedcontainers==2.4.0
iniconfig==2.3.0
packaging==24.0
pluggy==1.6.0
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.1
